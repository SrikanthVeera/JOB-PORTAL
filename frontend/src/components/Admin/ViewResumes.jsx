import React from 'react';
import { Typography, Box } from '@mui/material';

function ViewResumes() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h5" fontWeight={700} mb={3}>
        View Resumes (Stub)
      </Typography>
      {/* List resumes/applicants for a job, allow download */}
    </Box>
  );
}

export default ViewResumes; 